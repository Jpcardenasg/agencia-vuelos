package com.vuelosjanbi.tripBookingDetail.infrastructure.adapters.in;

import org.springframework.stereotype.Controller;

@Controller
public class TripBookingDetailConsoleAdapter {

  // @Autowired
  // private TripBookingDetailService tripBookingDetailService;

  // @Autowired
  // private CustomerService customerService;

  // private final String url = "jdbc:mysql://localhost:3307/vuelosjanpi";
  // private final String user = "root";
  // private final String password = "1324";

  // public void start(boolean jpa) {
  // if (jpa) {
  // System.out.println("Using JPA");
  // } else {
  // tripBookingDetailService = new TripBookingDetailService(
  // new MySQLTripBookingDetailRepository(url, user, password));
  // customerService = new CustomerService(new MySQLCustomerRepository(url, user,
  // password));
  // }

  // Scanner scanner = new Scanner(System.in);

  // while (true) {
  // System.out.println("1. Create Trip Booking Detail.");
  // System.out.println("2. Consult Trip Booking Detail.");

  // int choice = scanner.nextInt();
  // switch (choice) {
  // case 1:
  // // createTripBookingDetail(scanner);
  // break;
  // case 2:
  // // consultTripBookingDetailDetails(scanner, tripBookingDetailService);
  // break;
  // }
  // }

  // }

}
